"""Raport Word — export dashboard KA într-un `.docx`.

Refolosește generatorul deja existent din `app.modules.reports.generator`
și service-urile din `sales`, `stores`, `agents`, `products`, `tenants`.

Endpoint: POST /api/rapoarte/word — returnează docx streaming.
"""
